# Codex 后续任务建议

1. 增加题库 JSON 外置文件，不要把题目写死在 app.js 里。
2. 增加题库编辑器，方便手动调整 mbtiRule、objectRule、evidenceTags。
3. 增加本地测试记录列表，可以查看多次测试结果。
4. 增加小范围测试版匿名上传接口，但必须默认关闭，并在用户明确同意后上传。
5. 增加移动端结果页截图功能。
6. 增加更丰富的物件解释模板：不同 MBTI + 同一物件时，描述有细微变化。
